import 'package:flutter/material.dart';

class DisetujuiView extends StatelessWidget {
  const DisetujuiView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Halaman Disetujuicuy',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
